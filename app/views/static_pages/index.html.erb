<center>
  <% if flash[:notice] %>
    <%= flash[:notice] %>  
  <% end %>

  <nav class="navbar navbar-inverse navbar-fixed-top" style ='background-color: #99CCCC;'>
      <a class="navbar-brand" href="/" style = "font-family:Courier New;color:#333366">
        <b>Local Guide</b>
      </a>
  </nav>

  <video autoplay  poster="http://bit.ly/1HUHkcF" id="bgvid" loop>
  <!-- WCAG general accessibility recommendation is that media such as background video play through only once. Loop turned on for the purposes of illustration; if removed, the end of the video will fade in the same way created by pressing the "Pause" button  -->
<source src="http://bit.ly/1HUHkcF" type="video/webm">
<source src="//bit.ly/1HUHkcF" type="video/mp4">
</video>

  <h1>Guide Me</h1><br>
  <h3>Connect with locals during your trips</h3>
      <% if current_user == nil %>
        <%= link_to "Sign in with Facebook", "/auth/facebook", :class => "btn btn-info btn-sm"%>
      <% end %>

  <p>
    <%= form_tag(search_path, :method => "get", class: "navbar-form") do %>
  <form id="searchbox" action=""> 
    <%= text_field_tag :search, params[:search], placeholder: "Search Places" %>
    <%= submit_tag "Search", :name => nil %>
    <% end %>
    </form>
  </form> 
  </p>

</center>