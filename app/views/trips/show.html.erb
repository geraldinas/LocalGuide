<h1>Headed to: <%=@trip.city.name%></h1>
<p>Start date of trip: <%=@trip.start_date.to_formatted_s(:long_ordinal)%></p>

<p>End date of trip: <%=@trip.end_date.to_formatted_s(:long_ordinal)%></p>

<h3>Booked tours for your upcoming trip:</h3>

<p><%@trip.tours.each do |tour| %>
	<p><%=tour.activity.name%>
	<ul>
		<li>Start date of booked tour: <%=tour.start_time.to_formatted_s(:long_ordinal)%></li>
		<li>End date of booked tour: <%=tour.end_time.to_formatted_s(:long_ordinal)%></li>
	</ul>
<%end%>

<p><%= link_to "Find a Guide", trip_users_path(@trip) %></p>

*Button for all guides
*Button to edit
*Button to make a new trip
