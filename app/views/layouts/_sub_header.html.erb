<nav class="navbar navbar-default">
    <center>
      <p>
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
          <ul class="nav navbar-nav navbar-right"> 
            <% if current_user %>
              <%= link_to "Profile", "/profile", :class => "btn btn-info btn-sm" %>
               <%= link_to "Notifications", notifications_path, :class => "btn btn-info btn-sm" %>
               <span class="badge">
                 <%=current_user.pending_led_tours.count%>
               </span></a></li>

               <%= link_to "Public Info", user_path(current_user), :class => "btn btn-info btn-sm"%> 
               <%= link_to "Sign Out", signout_path, :class => "btn btn-info btn-sm"%>  
            <%end%>
          </ul>
        </div
      </p>
    </center>
</nav>