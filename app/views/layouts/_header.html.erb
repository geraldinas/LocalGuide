<nav class="navbar navbar-inverse navbar-fixed-top">
<div class="container">
  <div class="navbar-header">
    <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
      <span class="sr-only">Toggle navigation</span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
    </button>
    <a class="index-font navbar-brand " href="/">GuideMe</a>
  </div>
  <div id="navbar" class="collapse navbar-collapse">
    <ul class="nav navbar-nav navbar-right">
        <% if current_user && current_user.city && current_user.email && current_user.avatar %>
          <li><%= link_to "Profile", "/profile"  %></li>
          <li><%= link_to "Notifications", notifications_path  %></li>
          <li>
            <a>
              <span class="badge">
                 <%=current_user.pending_led_tours.count unless current_user.pending_led_tours.count < 0%>
              </span>
            </a>
          </li>
          <li><%= link_to "Public Info", user_path(current_user) %></li>
          <li><%= link_to "Sign Out", signout_path %></li>
      <%end%>
    </ul>
  </div>
</div>
</nav>