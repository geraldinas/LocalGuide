<%= form_for Review.new do |f| %>
	<%= f.hidden_field :guide_id, :value => @guide_id %>
	<%= f.hidden_field :reviewer_id, :value => reviewer_id %>
	<%= f.label      :review, "Review this guide!" %>
	<%= f.text_area  :review %>
	<%= f.submit %>
<% end %>





