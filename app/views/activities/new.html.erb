<%=form_for Activity.new do |f| %>
	<%= f.hidden_field :guide_id, :value => current_user.id%>
  <%= f.label :name%>
  <%= f.text_field :name%>	
	<%= f.label :description %>
	<%= f.text_field :description %> 
  <%= f.submit %>
<% end %>